# brew-dump-shell-autocommit-script
script that updates brew dump and generating commit message from git diff

# How to run
1) place script inside directory with git repository with brew-dump file
2) execute it
`` ./brew-dump.sh``
